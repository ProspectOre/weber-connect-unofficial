# Retired legacy entrypoint. Auto-merge is intentionally disabled; the active
# workflow is review-gate.yml.
name: Auto Merge (retired)
on: workflow_dispatch
permissions: {}
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy auto-merge workflow retired; use review-gate.yml.'
