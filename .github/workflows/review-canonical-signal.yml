# Generated adapter; edit scripts/review-gate/adapters in ProspectOre/dev-workspace.
name: Codex Review Signal
run-name: >-
  ${{ ((github.event.review.user.id == 199175422 && github.event.review.user.type == 'Bot' && github.event.review.user.login == 'chatgpt-codex-connector[bot]') || (github.event.comment.user.id == 199175422 && github.event.comment.user.type == 'Bot' && github.event.comment.user.login == 'chatgpt-codex-connector[bot]')) && (github.event.pull_request.number || github.event.issue.pull_request) && format('Codex review signal #{0}{1}', github.event.pull_request.number || github.event.issue.number, (github.event.action == 'deleted' || github.event.action == 'edited' || github.event.action == 'dismissed') && ' withdrawn' || '') || 'Ignored review signal' }}
on:
  pull_request_review:
    types: [submitted, edited, dismissed]
  issue_comment:
    types: [created, edited, deleted]
  pull_request_review_comment:
    types: [created, edited, deleted]
permissions: {}
jobs:
  signal:
    if: >-
      ((github.event.review.user.id == 199175422 && github.event.review.user.type == 'Bot' && github.event.review.user.login == 'chatgpt-codex-connector[bot]') || (github.event.comment.user.id == 199175422 && github.event.comment.user.type == 'Bot' && github.event.comment.user.login == 'chatgpt-codex-connector[bot]')) && (github.event.pull_request.number || github.event.issue.pull_request)
    runs-on: ubuntu-latest
    steps:
      - run: echo "Codex review changed; trusted gate will recompute API evidence."
