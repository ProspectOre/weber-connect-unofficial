# 3.2.1 candidate acceptance

The revised candidate preserves Bluetooth visibility before pairing cleanup
and preserves failure snapshots when returning from support. It passed 352
tests at 100% coverage. Its physical transport and endurance acceptance is
carried forward through the exact support-report-only amendment below; the
new report snapshot and return-navigation behavior is covered by targeted
regression tests.

## Initial candidate

Runtime fingerprint: `3c9c37b77b15769404f573866157c57d680134ff27558c0b794d71b7599fcec8`.

The support-report candidate passed 351 native tests with 100% combined
statement and branch coverage. HACS and Hassfest passed for the runtime PR;
regular review returned a positive verdict on commit `1518ee9045`.
Final release metadata and evidence still require review and CI on their head.

On Home Assistant 2026.9.1, the installed candidate rendered settings support,
reported a real Bluetooth discovery failure, retained multiple attempts in its
report, and returned to the original retry menu. Powering on the hub resolved
discovery. Active cancellation preserved cloud updates with zero failed
updates. Subsequent physical approval and cloud association succeeded, with
fresh probe telemetry. Every entity ID and friendly name matched the saved
baseline; the device and config entry remained the same.

A preserved rollback tree matched the installed 3.2.0 runtime. The old runtime
was not booted. No prior physical receipt has been relabelled or reused.

Sustained power loss cleared live readings and reported connection loss.
Power-on restored fresh probe telemetry without a reload. Recorder and Activity
exclude the timestamp entity while device status events remain visible.

The iPhone Air displayed the support link, four probe-name fields and Submit
without clipping. The unchanged candidate completed 3,920.174 seconds with 393
samples, zero new failed updates, zero disconnections, zero capture errors and
zero unexpected entry reloads. Maximum observed update gap was 11 seconds;
maximum sampled update age was 2.300871 seconds. No manual recovery occurred
during this uninterrupted run. Its baseline followed the deliberate power-off
and recovery test, whose 11 failures are retained separately in the raw capture.

The installed runtime fingerprint matched before and after the run. The sealed
capture digest is recorded in the adjacent physical JSON. The final runtime
amendment changes only report evidence capture and support return navigation;
Bluetooth transport, cloud transport, coordinator, entities, recovery behavior,
and pairing protocol are unchanged. `test_pairing_report_keeps_visibility_before_cleanup`
and the support-return regression tests provide the targeted evidence. Final
exact-head CI and regular review remain mandatory before merge and publication.
