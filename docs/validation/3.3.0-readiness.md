# 3.3.0 candidate acceptance

Runtime fingerprint: `3c9c37b77b15769404f573866157c57d680134ff27558c0b794d71b7599fcec8`.

The support-report candidate passed 351 native tests with 100% combined
statement and branch coverage. HACS and Hassfest passed for the runtime PR;
regular review returned a positive verdict on commit `1518ee9045`.
Final release metadata and evidence still require review and CI on their head.

On Home Assistant 2026.9.1, the installed candidate rendered settings support,
reported a real Bluetooth discovery failure, retained multiple attempts in its
report, and returned to the original retry menu. Powering on the hub resolved
discovery. Active cancellation preserved cloud updates with zero failed
updates. Subsequent physical approval and cloud association succeeded, with
fresh probe telemetry. Every entity ID and friendly name matched the saved
baseline; the device and config entry remained the same.

A preserved rollback tree matched the installed 3.2.0 runtime. The old runtime
was not booted. No prior physical receipt has been relabelled or reused.

Remaining: mobile UI, Activity filtering, sustained power loss and automatic
recovery, and a complete one-hour endurance capture. These are release gates,
not inferred successes. See the adjacent automated and physical JSON records.
